# DateTimeField - @zyncat/ui/datetime-field

Group: date-time
Docs: https://ui.zyncat.app/datetime-field

Calendar + segmented HH:MM; value 'YYYY-MM-DDTHH:mm'.

format 12h|24h, minuteStep.

```tsx
<DateTimeField label="Due at" value={v} onChange={setV} format="12h" minuteStep={15} />
```
