# Pagination - @zyncat/ui/pagination

Group: data-display
Docs: https://ui.zyncat.app/pagination

Cursor strip: mono range readout + prev/next pair.

range: [from,to], total?, hasPrev/hasNext, onPrev/onNext, ariaLabel.

```tsx
<Pagination ariaLabel="Rows" range={[from, to]} total={312} hasPrev hasNext onPrev={...} onNext={...} />
```
