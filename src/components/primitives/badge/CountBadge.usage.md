# CountBadge - @zyncat/ui/count-badge

Group: primitives
Docs: https://ui.zyncat.app/count-badge

Mono tabular count; `roll` animates digits like an odometer.

value: number | string.

```tsx
<CountBadge value="7 / 10" />
<CountBadge value={count} roll tone="info" />
```
