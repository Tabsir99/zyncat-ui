# ToggleTag - @zyncat/ui/toggle-tag

Group: data-display
Docs: https://ui.zyncat.app/tag

Many-of-many filter chip.

selected/onChange(next) or defaultSelected; icon, count.

```tsx
<ToggleTag selected={on} onChange={setOn} icon={<Icon name="star" />} count={48}>
  Starred
</ToggleTag>
```
