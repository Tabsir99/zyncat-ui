# OtpField - @zyncat/ui/otp-field

Group: forms
Docs: https://ui.zyncat.app/otp-field

Segmented one-time code; auto-advance, paste-to-fill.

length, group, error, size.

```tsx
<OtpField length={6} group={3} value={code} onChange={setCode} />
```
